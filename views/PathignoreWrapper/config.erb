<%
f = taglib("/lib/form")
f.block do
  f.entry(:title => 'Ignored paths',
          :field => 'ignored_paths',
          :description => 'Comma-separated, zsh-style globs') do
    f.textbox
  end
end
%>
